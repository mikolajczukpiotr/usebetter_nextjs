# Superuser connection used by Prisma CLI (migrations).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/usebetter

# Non-superuser connection for the app at runtime (required for RLS).
RUNTIME_DATABASE_URL=postgresql://app_user:app_password@localhost:5432/usebetter
